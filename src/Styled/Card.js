import styled from 'styled-components';
import { color,layout,space,border,flexbox } from 'styled-system';
export const Card = styled.div`
${color}
${layout}
${space}
${border}
${flexbox}

display:flex;


align-items:center;


`;